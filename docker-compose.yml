services:
  postgres:
    image: postgres:15-alpine
    container_name: retail-smart-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: retail_smart
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  app:    
    build:
      context: .
      dockerfile: Dockerfile
    image: retail-smart-app:latest
    restart: unless-stopped
    expose:
      - "3000"
      - "3001"
    environment:
      NODE_ENV: development
      PORT: 3000
      WS_PORT: 3001

      DATABASE_URL: postgres://postgres:postgres@postgres:5432/retail_smart
      DATABASE_URL_ADMIN: postgres://postgres:postgres@postgres:5432/retail_smart
      DATABASE_APP_ROLE: app_user

      NEXTAUTH_URL: http://localhost:8080
      NEXTAUTH_SECRET: 8f2a3c9d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0c1d2e3f4a5b6c7d8e9f0a1b

      NEXT_PUBLIC_APP_NAME: Retail Smart ERP
      NEXT_PUBLIC_BASE_DOMAIN: localhost
      NEXT_PUBLIC_LANDING_DOMAIN: localhost
      NEXT_PUBLIC_APP_DOMAIN: localhost

      ALLOWED_HOSTS: localhost,app.localhost

      NEXT_PUBLIC_APP_URL: http://localhost:8080

    depends_on:
      postgres:
        condition: service_healthy

  nginx:
    image: nginx:alpine
    container_name: retail-smart-nginx
    restart: unless-stopped
    ports:
      - "8080:80"
      - "3001:3001"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - app

volumes:
  postgres_data:

networks:
  default:
    name: retail-smart-network