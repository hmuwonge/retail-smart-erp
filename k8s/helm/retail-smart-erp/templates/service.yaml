apiVersion: v1
kind: Service
metadata:
  name: {{ include "retail-smart-erp.fullname" . }}
  labels:
    {{ include "retail-smart-erp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.service.wsPort }}
      targetPort: ws
      protocol: TCP
      name: ws
  selector:
    {{ include "retail-smart-erp.selectorLabels" . | nindent 4 }}
